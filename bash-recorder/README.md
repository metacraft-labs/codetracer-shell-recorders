# bash-recorder

Placeholder for the bashdb-based Bash recorder (M2+).

This directory will contain a fork/adaptation of [bashdb](https://bashdb.sourceforge.net/)
instrumented to emit CodeTracer trace events over the wire protocol consumed by
`ct-shell-trace-writer`.
