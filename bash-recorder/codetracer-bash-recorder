#!/usr/bin/env bash
# Wrapper script that provides a standard binary name for the bash recorder.
# The ct binary finds this via PATH using findTool("codetracer-bash-recorder").
exec "$(dirname "$0")/launcher.sh" "$@"
