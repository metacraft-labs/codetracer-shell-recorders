# zsh-recorder

Placeholder for the zshdb-based Zsh recorder (M8+).

This directory will contain a fork/adaptation of [zshdb](https://github.com/rocky/zshdb)
instrumented to emit CodeTracer trace events over the wire protocol consumed by
`ct-shell-trace-writer`.
